# Next.js
.next/
out/

# Vercel
.vercel

# Firebase emulator data (per-worktree, not shared)
.emulator-data/
.needs-seed

# Per-worktree dev port offset + generated emulator config (spec 0008)
.devports
firebase.runtime.json

# Environment variables
.env*
!.env.local.example

# Claude Code per-user state (settings, agent configs, transcripts) — never commit
.claude/

# Dependencies
node_modules/

# Test output
playwright-report/
test-results/
coverage/

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Misc
.DS_Store
dist
*.log
