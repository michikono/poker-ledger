# This repo is npm-only (single committed package-lock.json). engine-strict
# enforces the package.json "engines" range so an unsupported node/npm aborts
# installs rather than drifting silently. See spec 0029.
engine-strict=true
